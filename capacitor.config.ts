import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Generador de Codigo para Cruds',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
